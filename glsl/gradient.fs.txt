$HEADER$

uniform float alpha;
uniform float time;
uniform vec2 resolution;

void main( void ) {
  vec2 st = gl_FragCoord.xy / resolution;
  vec4 tex = texture2D(texture0, tex_coord0);
  gl_FragColor = vec4(st.x, st.y, sin(time / 10.0), alpha);
}
